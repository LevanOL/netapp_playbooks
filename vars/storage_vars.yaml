---
#
src_netapp: "{{ jcntap01p }}"
dest_netapp: "{{ jpntap02p }}"
src_hostname: "{{ src_netapp.ip }}"
dest_hostname: "{{ dest_netapp.ip }}"
src_username: "{{ src_netapp.username }}"
src_password: "{{ src_netapp.password }}"
dest_username: "{{ dest_netapp.username }}"
dest_password: "{{ dest_netapp.password }}"
aggr: "{{ src_netapp.src_aggr }}"
src_cluster: "{{ src_netapp.clustername }}"
src_vserver: "{{ src_netapp.src_svm }}"
dest_cluster: "{{ dest_netapp.clustername }}"
dest_vserver: "{{ dest_netapp.dest_svm }}"
size: 1
ostype: aix
volname: ""
lunname: ""
suffix: vol